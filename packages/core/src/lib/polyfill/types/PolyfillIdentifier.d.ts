import type { BrowserType } from '../../types/BrowserType';
import type { DeviceType } from '../../types/DeviceType';
import type { OSType } from '../../types/OSType';
export type PolyfillIdentifier = DeviceType | OSType | BrowserType;
