export const Polyfill = Object.freeze({
    devicePixelRatio: 'devicePixelRatio',
    innerHeight: 'innerHeight',
    innerWidth: 'innerWidth',
    matchMedia: 'matchMedia',
    maxTouchPoints: 'maxTouchPoints',
    platform: 'platform',
});
